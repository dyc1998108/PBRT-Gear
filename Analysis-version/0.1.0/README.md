# Analysis Version
The analysis version can only be run in **session**, which is different from converter version. It'll iterate all of the acquisitions belong to the target session, running pbrt there and packaging all of the results in a new **analysis** object under the target session.(Can be found through **(select project)->(select session)->Analyses**)
